% ============================================================
%  SKRIPSI – MASTER DOCUMENT
%  Compile this file to produce the full thesis.
%  Each bab-N.tex can also be compiled individually.
% ============================================================
\documentclass[12pt, a4paper, onecolumn, twoside, final]{report}
\raggedbottom

\usepackage{subfiles}   % ← enables per-chapter standalone compilation
\usepackage{hyphenat}
\usepackage{iftex}

% ── Encoding, Language & Fonts ───────────────────────────────
\ifXeTeX
    \usepackage{polyglossia}
    \setmainlanguage{bahasai}
    \usepackage{fontspec}
    \setmainfont{TeX Gyre Termes}   % Times New Roman equivalent
    \setsansfont{TeX Gyre Heros}    % Arial equivalent
    \setmonofont[Scale=0.88]{TeX Gyre Cursor}
\else
    \usepackage[utf8]{inputenc}
    \usepackage[T1]{fontenc}
    \usepackage[indonesian]{babel}
    \usepackage{mathptmx}           % Times New Roman equivalent
    \usepackage{courier}            % monospace (texttt, listings)
\fi

% ── Page geometry (A4, UI 2017 standard: 3 cm all sides) ─────
\usepackage[
  a4paper,
  headheight = 14.5pt,
  top    = 3cm,
  bottom = 3cm,
  left   = 3cm,
  right  = 3cm
]{geometry}

% -- Header & footer (mirrors latex-ta-ui/uithesis.sty) --------
\usepackage{fancyhdr}
\newcommand{\uifootermark}{%
  \footnotesize\sffamily\bfseries Universitas Indonesia%
}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhf{}
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyfoot[C]{\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}
\fancypagestyle{onlypage}{%
  \fancyhf{}%
  \fancyhead[LE,RO]{\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}
\fancypagestyle{first-pages}{%
  \fancyhf{}%
  \fancyhead[LE,RO]{\thepage}%
  \fancyfoot[LE,RO]{\uifootermark}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}
\fancypagestyle{standard}{%
  \fancyhf{}%
  \fancyhead[LE,RO]{\thepage}%
  \fancyfoot[LE,RO]{\uifootermark}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}

% -- Appendix helpers ------------------------------------------
\newcounter{lampno}
\newcommand{\setlampirannumbering}{%
  \renewcommand{\thelampno}{Lampiran~\arabic{lampno}}%
  \renewcommand{\thesubsection}{L\arabic{lampno}.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%
  \renewcommand{\thefigure}{L\arabic{lampno}.\arabic{figure}}%
  \renewcommand{\thetable}{L\arabic{lampno}.\arabic{table}}%
}
\newcommand{\lampiran}[1]{%
  \clearpage
  \setlampirannumbering
  \refstepcounter{lampno}%
  \setcounter{subsection}{0}\setcounter{figure}{0}\setcounter{table}{0}%
  \section*{Lampiran~\arabic{lampno}\quad #1}%
  \thispagestyle{plain}%
  \addcontentsline{toc}{section}{Lampiran~\arabic{lampno}\texorpdfstring{\quad}{ }#1}%
}

% ── Line spacing & paragraph ─────────────────────────────────
\usepackage{setspace}
\setstretch{1.5}   % 1,5 spasi (sesuai pedoman UI / format guide)
\setlength{\parindent}{1.27cm}
\setlength{\parskip}{0pt}

% ── Justification & hyphenation ──────────────────────────────
\usepackage{microtype}
\sloppy

% ── Headings ─────────────────────────────────────────────────
\usepackage{titlesec}

% Chapter heading: "BAB <n>" pada baris pertama, judul (UPPERCASE) di baris
% kedua. (Lampiran memakai \chapter*{LAMPIRAN} sebagai pembatas, lalu tiap
% lampiran jadi subbab — lihat blok LAMPIRAN di bawah.)
\titleformat{\chapter}[display]
  {\centering\rmfamily\bfseries\large}
  {BAB~\thechapter}
  {0.6em}{\MakeUppercase}
\titlespacing*{\chapter}{0pt}{0pt}{1.5em}

\titleformat{\section}
  {\rmfamily\bfseries\normalsize}{\thesection}{1em}{}
\titlespacing*{\section}{0pt}{1.2em}{0.6em}

\titleformat{\subsection}
  {\rmfamily\bfseries\normalsize}{\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{1em}{0.5em}

\titleformat{\subsubsection}
  {\rmfamily\bfseries\normalsize}{\thesubsubsection}{1em}{}
\titlespacing*{\subsubsection}{0pt}{0.8em}{0.4em}

% ── Tables ───────────────────────────────────────────────────
\usepackage{longtable, booktabs, array, multirow}
\usepackage{calc}
\usepackage{tabularx}

% ── Math ─────────────────────────────────────────────────────
\usepackage{amsmath, amssymb}

% ── Graphics ─────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{adjustbox}   % scale oversized diagrams to fit width AND page height
\usepackage{float}       % [H] placement: show a figure exactly where it is declared
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,keepaspectratio}

% ── TikZ + pgfplots ──────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds}
\usepackage{pgfplots}
\usepgfplotslibrary{groupplots}
\pgfplotsset{compat=1.18}

% ── Lists ────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[enumerate,1]{
  label      = \arabic*.,
  leftmargin = 1.27cm,
  itemsep    = 4pt,
  parsep     = 0pt,
  topsep     = 4pt
}
\setlist[enumerate,2]{
  label      = \alph*.,
  leftmargin = 1.27cm,
  itemsep    = 3pt,
  parsep     = 0pt,
  topsep     = 2pt
}

% ── Code listings (prompt templates & Cypher) ───────────────
\usepackage{listings}
\lstdefinestyle{promptbox}{
  basicstyle      = \ttfamily\scriptsize,
  breaklines      = true,
  breakatwhitespace = false,
  columns         = fullflexible,
  keepspaces      = true,
  showstringspaces = false,
  frame           = single,
  framesep        = 5pt,
  xleftmargin     = 10pt,
  xrightmargin    = 2pt,
  aboveskip       = 8pt,
  belowskip       = 8pt,
}
\lstdefinestyle{cypherbox}{
  style        = promptbox,
  morecomment  = [l]{//},
  morecomment  = [s]{/*}{*/},
  keywordstyle = \bfseries,
  morekeywords = {MATCH,OPTIONAL,MERGE,CREATE,CONSTRAINT,FOR,REQUIRE,IS,UNIQUE,
                  WHERE,WITH,RETURN,SET,UNWIND,CALL,YIELD,DETACH,DELETE,AS,AND,
                  OR,NOT,IN,CASE,WHEN,THEN,ELSE,END,STARTS,CONTAINS,ORDER,BY,
                  LIMIT,DISTINCT,coalesce,count,sum,avg,collect,toFloat,length,
                  nodes,type,trim,labels,relationshipTypes},
}

% ── Hyperlinks ───────────────────────────────────────────────
\usepackage[hidelinks]{hyperref}

% ── Bibliography (biblatex, author-year) ─────────────────────
%  Two-author connector rendered as "dan"; 3+ authors as "et al."
% csquotes has no quote style for Indonesian/bahasai; loaded bare it emits a
% placeholder ("?") around every biblatex title via \mkbibquote. Force the
% English style so titles get proper “ ” quotes. (\DeclareLanguageMapping above
% only remaps biblatex localization strings, NOT csquotes — different package.)
\usepackage[autostyle=false]{csquotes}   % recommended companion for babel + biblatex
\setquotestyle{english}
\usepackage[
  backend     = biber,
  style       = authoryear,
  maxcitenames = 2,
  mincitenames = 1,
  maxbibnames = 99,
  uniquename  = init,    % avoids the giveninits/uniquename=full conflict warning
  giveninits  = true,
  dashed      = false,
  doi         = false,
  isbn        = false,
  url         = false
]{biblatex}
% biblatex ships no Indonesian localization (.lbx); the 'language=english'
% option alone does NOT override babel, so map the document language to
% english.lbx. This resolves every "string untranslated" / "fallback
% definition for \mkbibdate*" warning at once (et al., in, pages, volume,
% edition, month names, ordinals).
\DeclareLanguageMapping{indonesian}{english}
\DeclareLanguageMapping{bahasai}{english}
\addbibresource{\subfix{config/references.bib}}  % \subfix: path resolves for both the main build and standalone subfile compiles

\renewcommand*{\finalnamedelim}{\addspace dan\addspace}  % two authors -> "dan"

% ============================================================
\begin{document}

% Penanda outline Overleaf: \iffalse...\fi membuat TeX melewati perintah
% sectioning (tidak tercetak & tidak memengaruhi penomoran), tetapi pemindai
% File Outline Overleaf tetap menampilkannya sehingga master file ini bisa
% dinavigasi per-bab/lampiran. Judul bab/lampiran asli tetap di tiap subfile.

% ── HALAMAN DEPAN (front matter, angka Romawi) ───────────────
\iffalse\chapter{Halaman Depan}\fi
\pagenumbering{roman}
\pagestyle{onlypage}
\subfile{src/00-frontMatter/front-matter}

\pagestyle{first-pages}
\tableofcontents
\listoffigures
\listoftables
\clearpage

% ── ISI (angka Arab) ─────────────────────────────────────────
\pagenumbering{arabic}
\pagestyle{standard}

% ── BAB 1 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 1: Pendahuluan}\fi
\setcounter{chapter}{0}
\subfile{src/01-body/bab-1}

% ── BAB 2 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 2: Tinjauan Literatur}\fi
\setcounter{chapter}{1}
\subfile{src/01-body/bab-2}

% ── BAB 3 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 3: Metodologi Penelitian}\fi
\setcounter{chapter}{2}
\subfile{src/01-body/bab-3}

% ── BAB 4 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 4: Perancangan dan Pengembangan Artefak}\fi
\setcounter{chapter}{3}
\subfile{src/01-body/bab-4}

% ── BAB 5 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 5: Pembahasan}\fi
\setcounter{chapter}{4}
\subfile{src/01-body/bab-5}

% ── BAB 6 ────────────────────────────────────────────────────
\iffalse\chapter{BAB 6: Penutup}\fi
\setcounter{chapter}{5}
\subfile{src/01-body/bab-6}

% ── DAFTAR PUSTAKA ───────────────────────────────────────────
\iffalse\chapter{Daftar Pustaka}\fi
\printbibliography[heading=bibintoc, title={Daftar Pustaka}]

% ── LAMPIRAN ─────────────────────────────────────────────────
% Halaman pembatas "LAMPIRAN" setingkat bab (masuk Daftar Isi). Tiap lampiran
% dibuka dengan \lampiran{judul} → subbab tak bernomor "Lampiran N  judul"
% (pakai \section* + entri TOC manual, sehingga "Lampiran N" mengalir sebagai
% teks dan tidak terpotong kotak nomor TOC). Subbab/gambar/tabel di dalamnya
% diberi prefiks "L<n>." dan direset di tiap lampiran.
\clearpage
\chapter*{LAMPIRAN}
\addcontentsline{toc}{chapter}{LAMPIRAN}

\iffalse\section{Lampiran 1: Arsitektur KG Review App}\fi
\subfile{src/99-backMatter/lampiran-1}
\iffalse\section{Lampiran 2: Visualisasi Knowledge Graph per Mata Pelajaran}\fi
\subfile{src/99-backMatter/lampiran-2}
\iffalse\section{Lampiran 3: Templat Prompt}\fi
\subfile{src/99-backMatter/lampiran-3}
\iffalse\section{Lampiran 4: Daftar Relasi Lintas-Buku dan Hasil Validasi Fase 2}\fi
\subfile{src/99-backMatter/lampiran-4}
\iffalse\section{Lampiran 5: Kueri Cypher}\fi
\subfile{src/99-backMatter/lampiran-5}
\iffalse\section{Lampiran 6: Daftar Missing Triples}\fi
\subfile{src/99-backMatter/lampiran-6}
\iffalse\section{Lampiran 7: Daftar Buku Teks (Korpus Penelitian)}\fi
\subfile{src/99-backMatter/lampiran-7}
\iffalse\section{Lampiran 8: Profil dan Kualifikasi Pakar}\fi
\subfile{src/99-backMatter/lampiran-8}
\iffalse\section{Lampiran 9: Daftar Relasi Berlabel Wrong}\fi
\subfile{src/99-backMatter/lampiran-9}
\iffalse\section{Lampiran 10: Pembagian Tugas dan Kontribusi Anggota}\fi
\subfile{src/99-backMatter/lampiran-10}

\end{document}
